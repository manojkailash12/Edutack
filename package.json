{
  "name": "edutack-netlify",
  "version": "1.0.0",
  "description": "Edutack - Complete Education Management System on Netlify",
  "main": "netlify/functions/api.js",
  "scripts": {
    "build": "cd Frontend && npm install && CI=false npm run build",
    "dev": "cd Frontend && npm start",
    "backend": "cd Backend && npm start",
    "install-all": "npm install && cd Frontend && npm install && cd ../Backend && npm install",
    "deploy": "netlify deploy --prod --dir=Frontend/build --functions=netlify/functions"
  },
  "keywords": [
    "education",
    "management",
    "system",
    "netlify",
    "serverless",
    "react",
    "nodejs"
  ],
  "author": "Edutack Team",
  "license": "MIT",
  "dependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "repository": {
    "type": "git",
    "url": "your-github-repo-url"
  }
}